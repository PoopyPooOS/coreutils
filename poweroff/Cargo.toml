[package]
name = "poweroff"
version = "0.1.0"
edition = "2021"

[dependencies]
ipc-init = { path = "../../init/ipc-init" }
nix = { version = "0.29.0", features = ["reboot"] }

[profile.release]
opt-level = "z"
lto = true
strip = true
